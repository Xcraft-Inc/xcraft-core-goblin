'use strict';

const CacheParams = require('./cacheParams.js');

const cacheQuestParams = new CacheParams();
const cacheReduceParams = new CacheParams();

module.exports = {
  cacheQuestParams,
  cacheReduceParams,
};
